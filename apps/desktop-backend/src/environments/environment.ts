export const environment = {
  production: false,
  // Where the .cfg file is stored as well
  userDataPath: "Electron",
  // The parent directory of user data I think
  appDataPath: "Roaming",
  appResourcesPath: "resources",
  logDirectory: "logs",
};
